# MercadoLibre App Credentials
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git!

# Public - Exposed to client (safe to use in browser)
NEXT_PUBLIC_MELI_APP_ID=your_app_id_here

# Private - Server-side only (NEVER add NEXT_PUBLIC_ prefix)
MELI_CLIENT_SECRET=your_client_secret_here

# OAuth Redirect URI
NEXT_PUBLIC_REDIRECT_URI=http://localhost:3000/api/auth/callback

# Redis Cache (Upstash Redis or any Redis-compatible service)
# Used for caching OAuth tokens and enriched trends data
# Format: redis://[user]:[password]@[host]:[port]
# Production: Automatically set when linking Vercel KV database
REDIS_URL=redis://default:your_redis_password@your-redis-host:6379
